spring:
  jpa:
    hibernate:
      ddl-auto:
        create
  datasource:
    url:
      jdbc:mysql://localhost:3306/jl_db
    username:
      root
    password:
      7F9K1N3O
    driver-class-name:
      com.mysql.cj.jdbc.Driver

server:
  port: 8090
spring.jpa.show-sql: true