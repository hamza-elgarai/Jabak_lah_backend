spring:
  jpa:
    hibernate:
      ddl-auto:
        create-drop
  datasource:
    url:
      jdbc:mysql://localhost:3306/jl_db
    username:
      root
    password:
      Massaq51354
    driver-class-name:
      com.mysql.cj.jdbc.Driver

server:
  port: 8090
spring.jpa.show-sql: true